<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>com.ordering</groupId>
        <artifactId>backend</artifactId>
        <version>${revision}</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>pay-biz</artifactId>
    <packaging>jar</packaging>

    <name>${project.artifactId}</name>
    <description>
        pay 模块，我们放支付业务，提供业务的支付能力。
        例如说：商户、应用、支付、退款等等
    </description>

    <dependencies>
        <dependency>
            <groupId>com.ordering</groupId>
            <artifactId>pay-api</artifactId>
            <version>${revision}</version>
        </dependency>

        <!-- 业务组件 -->
<!--        <dependency>-->
<!--            <groupId>com.ordering</groupId>-->
<!--            <artifactId>app-spring-boot-starter-biz-operatelog</artifactId>-->
<!--        </dependency>-->
        <dependency>
            <groupId>com.ordering</groupId>
            <artifactId>starter-tenant</artifactId>
        </dependency>

        <!-- Web 相关 -->
        <dependency>
            <groupId>com.ordering</groupId>
            <artifactId>starter-security</artifactId>
        </dependency>

<!--        <dependency>-->
<!--            <groupId>com.ordering</groupId>-->
<!--            <artifactId>app-spring-boot-starter-desensitize</artifactId>-->
<!--        </dependency>-->


        <dependency>
            <groupId>com.ordering</groupId>
            <artifactId>starter-redis</artifactId>
        </dependency>


        <!-- 消息队列相关 -->
        <dependency>
            <groupId>com.ordering</groupId>
            <artifactId>starter-mq</artifactId>
        </dependency>

        <!-- Test 测试相关 -->
        <dependency>
            <groupId>com.ordering</groupId>
            <artifactId>starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- 工具类相关 -->
        <dependency>
            <groupId>com.ordering</groupId>
            <artifactId>starter-excel</artifactId>
        </dependency>



    </dependencies>

</project>
